# @kakoyo/dsh-system-prompt bundle patch: one row, so this package can be
# installed on its own via `dsh plugin --profile <name> add @kakoyo/dsh-system-prompt`.

- insert:
    - id: kakoyo-system-prompt
      name: '@kakoyo/dsh-system-prompt'
